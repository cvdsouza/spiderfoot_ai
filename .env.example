# SpiderFoot environment configuration
# Copy this file to .env and edit the values:
#   cp .env.example .env

# ── Admin credentials ─────────────────────────────────────────────────────────
# These are used to create the initial administrator account on first startup.
# Once created, the password can be changed from the UI (Settings → Change Password).
# Changing these values after the first run has no effect — use the UI instead.

SPIDERFOOT_ADMIN_USER=admin
SPIDERFOOT_ADMIN_PASSWORD=SpiderFoot
